<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>#{title}</title>
    style
        include ../static/style.css
</head>
<body style="background: url(../static/Empty-Gym-scaled.jpg) no-repeat center;">
    <nav>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/">About</a></li>
            <li><a href="/">Contact</a></li>
        </ul>
    </nav>
    <div class="container">
        <p>
            | <!-- if we are using plain html in pug and in that if we have multiple lines we have to use pipe (|) for using varible -->
            | <!-- #{paragraph} -->
        </p>
        
        <h1>#{heading}</h1>
        <form action="/" method="post" id="form">
            <input type="text" name="name" id="name" placeholder="Enter your name">
            <input type="number" name="age" id="age" placeholder="Enter your age">
            <input type="text" name="gender" id="gender" placeholder="Enter your gender">
            <textarea name="address" id="address" cols="30" rows="10" placeholder="Enter your Address"></textarea>
            <textarea name="more" id="address" cols="30" rows="10" placeholder="Tell us more about yourself"></textarea>
            <button id="btn" type="submit">Submit</button>
        </form>
    </div>
</body>
</html>